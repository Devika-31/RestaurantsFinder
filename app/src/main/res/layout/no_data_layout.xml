<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center"
    android:orientation="vertical">

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/ivNoData"
        android:layout_width="match_parent"
        android:layout_height="@dimen/_120sdp"
        android:scaleType="centerInside"
        app:srcCompat="@drawable/ic_no_data_placeholder" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tvTitle"
         android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="-30dp"
        android:gravity="center_horizontal"
        android:text="@string/insufficient_data"
        android:textColor="@color/cornflower_blue" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tvSubTitle"
         android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/_6sdp"
        android:ellipsize="end"
        android:gravity="center_horizontal"
        android:maxLines="2"
        android:paddingHorizontal="@dimen/_20ssp"
        android:text="@string/there_is_not_enough_data_check_your_connection_or_try_again"
        android:textColor="@color/fiord" />
</LinearLayout>